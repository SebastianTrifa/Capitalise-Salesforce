trigger sevendaysdate on Opportunity (before insert, before update, after update) {        
   /* OpportunityTriggerHelper handler = new OpportunityTriggerHelper();
    if(Trigger.isInsert && Trigger.isBefore) {
        handler.sevendaysrule(trigger.new);
    }
    else if(Trigger.isUpdate && Trigger.isBefore) {
        handler.sevendaysrule(trigger.new);
    }
    else if(Trigger.isUpdate && Trigger.isAfter) {
        handler.webhook(trigger.new, trigger.oldMap);
    }*/    
}